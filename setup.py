# Ai_uni/setup.py
from setuptools import setup, find_packages

setup(
    name='Uni_AI',           # Name to import
    version='0.1',
    packages=find_packages(), # Automatically finds your modules
)
